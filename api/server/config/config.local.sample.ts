export const localConfig = {
  google: {
    api: {
      key: '<API key>',
    },
    oauth: {
      client_id: '<OAuth app client id>',
      secret: '<OAuth app secret>',
      login_style: 'redirect',
      scope: ['email', 'profile']
    }
  }
};
